# Production Environment Variables for Render.com
# Set these in your Render dashboard under Environment Variables

# Database - Use your production PostgreSQL URL from Render
DATABASE_URL="postgresql://username:password@host:port/database"

# JWT Secret - Use a strong, unique secret for production
JWT_SECRET="your-super-secure-jwt-secret-here"

# Server Configuration
PORT=5000
NODE_ENV=production

# Frontend URL (optional, but recommended for explicit CORS control)
FRONTEND_URL="https://abc-gymnastics-1.onrender.com"

# Note: Stripe keys removed since we're using simulated payments
# If you want to re-enable Stripe later, add:
# STRIPE_SECRET_KEY="sk_live_..."
# STRIPE_PUBLISHABLE_KEY="pk_live_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
